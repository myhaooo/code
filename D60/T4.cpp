#include <iostream>
#include <vector>
using namespace std;

int main() {
	#ifndef LOCAL
    freopen("binary.in", "r", stdin);
    freopen("binary.out", "w", stdout);
#endif
    int n, Q;
    cout<<"1";

    return 0;
}

